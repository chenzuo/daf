@using DAF.Web;
@{
    var redirect = Request.QueryString["redirect"];
    if (redirect.IsEmpty())
    {
        redirect = Href("~/");
    }

    AuthHelper.SignOff();
    Context.RedirectLocal(redirect);
}

